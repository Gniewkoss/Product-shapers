# Skopiuj na host produkcyjny jako cms/.env (nie commituj).
# Przy proxy Netlify adres witryny użytkownika = domena Netlify (nie URL backendu Render).

DATABASE_URI=postgresql://user:pass@host:5432/payload
PAYLOAD_SECRET=min-32-znakow-losowych

# Dokładnie adres z paska przeglądarki (bez końcowego /):
PAYLOAD_PUBLIC_SERVER_URL=https://twoja-strona.netlify.app

# Ta sama domena (+ opcjonalnie deploy-preview Netlify, po przecinku):
FRONTEND_ORIGINS=https://twoja-strona.netlify.app

# Pierwsza instalacja na pustej bazie często true; później można false — patrz DEPLOY-NETLIFY.md
PAYLOAD_DATABASE_PUSH=true
